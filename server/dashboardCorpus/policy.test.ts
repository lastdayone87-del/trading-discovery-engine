import test from 'node:test';import assert from 'node:assert/strict';import {classifyDashboardCorpus} from './policy';
test('dashboard corpus separation maps admission states without serving authority',()=>{assert.equal(classifyDashboardCorpus('NOT_EVALUATED').corpus,'DISCOVERY_CANDIDATES');assert.equal(classifyDashboardCorpus('WITHHELD_INVESTIGATING').corpus,'INVESTIGATING');assert.equal(classifyDashboardCorpus('ADMITTED_REVIEW').corpus,'REVIEW');assert.equal(classifyDashboardCorpus('ADMITTED_CONFIRMED').corpus,'CONFIRMED');assert.equal(classifyDashboardCorpus('WITHHELD_OPERATIONAL_FAILURE').corpus,'WITHHELD');assert.equal(classifyDashboardCorpus('ADMITTED_CONFIRMED').servingAuthority,false);});
