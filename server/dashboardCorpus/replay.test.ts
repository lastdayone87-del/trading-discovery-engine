import test from 'node:test';import assert from 'node:assert/strict';import {replayDashboardCorpusEvents} from './replay';import {DASHBOARD_CORPUS_POLICY_VERSION} from './policy';
const event=(id:string,expected:number,corpus:string)=>({id,occurred_at:`2026-01-0${id}T00:00:00Z`,channel_id:'c',decision_id:`d${id}`,expected_projection_version:expected,payload:{corpus,admissionState:'NOT_EVALUATED',evidenceChecksum:`x${id}`,reasonCodes:[]},policy_version:DASHBOARD_CORPUS_POLICY_VERSION});
test('dashboard corpus replay is deterministic and gap checked',()=>{const events=[event('2',1,'CONFIRMED'),event('1',0,'DISCOVERY_CANDIDATES')];assert.equal(replayDashboardCorpusEvents(events).get('c')?.corpus,'CONFIRMED');assert.throws(()=>replayDashboardCorpusEvents([event('2',1,'CONFIRMED')]),/REPLAY_GAP/);});
