import test from 'node:test';import assert from 'node:assert/strict';import {GAP_ACTIONS,planGapSpecificAction} from './gapSpecificInvestigation';import type {CreatorFocusDecision} from './evidenceEngine/classifierV4';
const decision={classifierVersion:'v',policyVersion:'p',proposedStatus:'UNCERTAIN',effectiveStatus:'UNCERTAIN',probability:.4,lowerConfidenceBound:.2,reasonCodes:[],admissionRecommendation:{action:'WITHHOLD',reasonCodes:[]},automaticTerminalAuthority:false,stages:[{stage:'DOCUMENT_COVERAGE',disposition:'ABSTAIN',reasonCodes:['DOCUMENT_COVERAGE_INSUFFICIENT'],metrics:{}}]} as CreatorFocusDecision;
test('catalog includes full governed vocabulary and explicitly unavailable adapters',()=>{assert.equal(GAP_ACTIONS.length,13);assert.equal(GAP_ACTIONS.find(a=>a.action==='FETCH_TRANSCRIPT_EXCERPT')?.adapter,'UNAVAILABLE');});
test('planner never selects unavailable or over-budget actions',()=>{const p=planGapSpecificAction({decision,providerQuotaRemaining:101,caseQuotaRemaining:101,deadlineRemainingMs:5000,reviewCapacity:0});assert.equal(p.selectedAction,'FETCH_RECENT_VIDEO_SAMPLE');assert.notEqual(p.adapter,'UNAVAILABLE');const blocked=planGapSpecificAction({decision,providerQuotaRemaining:0,caseQuotaRemaining:0,deadlineRemainingMs:1,reviewCapacity:0});assert.equal(blocked.selectedAction,undefined);assert.ok(blocked.reasonCodes.includes('OPERATIONALLY_BLOCKED'));});
