import assert from 'node:assert/strict';import test from 'node:test';import {replayAdmission,type AdmissionReplayEvent} from './projector';
const event=(override:Partial<AdmissionReplayEvent>={}):AdmissionReplayEvent=>({eventKey:'e1',channelId:'c1',priorState:'NOT_EVALUATED',resultingState:'WITHHELD_INVESTIGATING',expectedProjectionVersion:0,occurredAt:'2026-01-01T00:00:00Z',decisionId:'d1',policyVersion:'v1',evidenceChecksum:'x',reasonCodes:[],...override});
test('admission replay is deterministic and version checked',()=>{const events=[event(),event({eventKey:'e2',priorState:'WITHHELD_INVESTIGATING',resultingState:'ADMITTED_CONFIRMED',expectedProjectionVersion:1,occurredAt:'2026-01-02T00:00:00Z',decisionId:'d2'})];assert.deepEqual(replayAdmission(events),replayAdmission([...events].reverse()));assert.equal(replayAdmission(events).get('c1')?.state,'ADMITTED_CONFIRMED');});
test('admission replay fails closed on gaps or stale state',()=>assert.throws(()=>replayAdmission([event({expectedProjectionVersion:1})]),/ADMISSION_REPLAY_CONFLICT/));
