export const REVIEW_ELIGIBILITY_POLICY_VERSION='review-eligibility-v2-shadow-1';
export type ReviewEligibilityStatus='ELIGIBLE'|'NOT_ELIGIBLE'|'DEFERRED';
export interface ReviewEligibilityInput{classificationStatus:string;investigationState:string;plausibleTradingHypothesis:boolean;evidenceSufficient:boolean;independentEvidence:boolean;countryAllowed:boolean;operationalFailure:boolean;providerDegraded:boolean;unsupportedLanguage:boolean;terminalDecision:boolean}
export interface ReviewEligibilityDecision{status:ReviewEligibilityStatus;reasonCodes:string[];servingAuthority:false;policyVersion:string}
export function evaluateReviewEligibilityV2(input:ReviewEligibilityInput):ReviewEligibilityDecision{let status:ReviewEligibilityStatus='NOT_ELIGIBLE',reasonCodes:string[];if(!input.countryAllowed)reasonCodes=['COUNTRY_POLICY_TERMINAL'];else if(input.terminalDecision||['TRADING_CONFIRMED','NON_TRADING','HUMAN_REJECTED'].includes(input.classificationStatus))reasonCodes=['TERMINAL_DECISION_NOT_REVIEWABLE'];else if(input.operationalFailure)reasonCodes=['OPERATIONAL_FAILURE_NOT_REVIEW'];else if(input.investigationState==='ACTIVE') {status='DEFERRED';reasonCodes=['INVESTIGATION_NOT_EXHAUSTED'];}else if(input.providerDegraded){status='DEFERRED';reasonCodes=['PROVIDER_RECOVERY_REQUIRED'];}else if(input.unsupportedLanguage){status='DEFERRED';reasonCodes=['LANGUAGE_CAPABILITY_REQUIRED'];}else if(!input.plausibleTradingHypothesis)reasonCodes=['PLAUSIBLE_TRADING_HYPOTHESIS_REQUIRED'];else if(!input.evidenceSufficient||!input.independentEvidence){status='DEFERRED';reasonCodes=['EVIDENCE_ACQUISITION_REQUIRED'];}else if(input.classificationStatus==='UNCERTAIN'&&['UNRESOLVED','REVIEW_ELIGIBLE','NEEDS_REVIEW'].includes(input.investigationState)){status='ELIGIBLE';reasonCodes=['AMBIGUITY_REQUIRES_HUMAN_JUDGMENT'];}else reasonCodes=['REVIEW_POLICY_ABSTAINED'];return {status,reasonCodes,servingAuthority:false,policyVersion:REVIEW_ELIGIBILITY_POLICY_VERSION};}
