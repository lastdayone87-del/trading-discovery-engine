import test from 'node:test';import assert from 'node:assert/strict';import {replayReviewEligibilityEvents} from './replay';import {REVIEW_ELIGIBILITY_POLICY_VERSION} from './policy';
test('review eligibility replay is deterministic, versioned and gap checked',()=>{const one={id:'1',occurred_at:'2026-01-01',channel_id:'c',decision_id:'d1',expected_projection_version:0,payload:{status:'DEFERRED',evidenceChecksum:'x',reasonCodes:[]},policy_version:REVIEW_ELIGIBILITY_POLICY_VERSION},two={...one,id:'2',occurred_at:'2026-01-02',decision_id:'d2',expected_projection_version:1,payload:{status:'ELIGIBLE',evidenceChecksum:'y',reasonCodes:[]}};assert.equal(replayReviewEligibilityEvents([two,one]).get('c')?.status,'ELIGIBLE');assert.throws(()=>replayReviewEligibilityEvents([two]),/REPLAY_GAP/);assert.throws(()=>replayReviewEligibilityEvents([{...one,policy_version:'old'}]),/VERSION_MISMATCH/);});
