import type {EvidenceDocumentObservation} from './documentTypes';
/** Corroboration-safe families. Search context and unresolved playlist containers
 * are observable but cannot independently corroborate creator focus. */
export function assessDocumentIndependence(documents:EvidenceDocumentObservation[]){const eligible=documents.filter(document=>document.documentType!=='SEARCH_MATCH_CONTEXT'&&!document.documentType.startsWith('PLAYLIST_')),families=new Map<string,EvidenceDocumentObservation[]>();for(const document of eligible)families.set(document.sourceFamilyId,[...(families.get(document.sourceFamilyId)||[]),document]);return {independentFamilyCount:families.size,excludedSearchMatches:documents.filter(d=>d.documentType==='SEARCH_MATCH_CONTEXT').length,excludedUnresolvedPlaylists:documents.filter(d=>d.documentType.startsWith('PLAYLIST_')).length,families:[...families.entries()].map(([sourceFamilyId,members])=>({sourceFamilyId,documentKeys:members.map(member=>member.documentKey).sort()})).sort((a,b)=>a.sourceFamilyId.localeCompare(b.sourceFamilyId))};}
