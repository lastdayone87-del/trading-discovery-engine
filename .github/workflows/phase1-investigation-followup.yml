name: Phase1InvestigationFollowup

on:
  push:
    branches:
      - fix/review-state-semantics-phase1
    paths:
      - .github/workflows/phase1-investigation-followup.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/review-state-semantics-phase1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/phase1_investigation_followup.py
      - run: |
          git config user.name phase1-autofix
          git config user.email phase1-autofix@users.noreply.github.com
          git rm .github/workflows/phase1-investigation-followup.yml scripts/phase1_investigation_followup.py
          git add server/investigationWorkflow.ts
          git commit -m 'fix: keep repaired investigations machine-owned'
          git push origin HEAD:fix/review-state-semantics-phase1
